{
    "non-interactive": true,
    "dry-run": false,
    "verbose": true,
    "buildCommand": "gulp && gulp test:e2e && cp -a .tmp/build build",
    "npm": {
      "publish": true
    },
    "github": {
      "release": false,
      "releaseName": "Release %s",
      "tokenRef": "GITHUB_TOKEN"
    },
    "dist": {
      "repo": false,
      "baseDir": "."
    }
}
